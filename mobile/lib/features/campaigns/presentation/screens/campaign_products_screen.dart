import 'package:flutter/material.dart';

// This file is deprecated and should be deleted.
// Kept temporarily to prevent compile errors if deletion fails.
class CampaignProductsScreen extends StatelessWidget {
  const CampaignProductsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
