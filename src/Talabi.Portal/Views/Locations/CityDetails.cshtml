@model Talabi.Core.Entities.City
@using Talabi.Portal.Services
@inject ILocalizationService LocalizationService
@{
    ViewData["Title"] = LocalizationService.GetString("Districts");
}

<div class="container p-4">
     <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb mb-1">
                    <li class="breadcrumb-item"><a asp-action="Index">@LocalizationService.GetString("Countries")</a></li>
                    <li class="breadcrumb-item"><a asp-action="CountryDetails" asp-route-id="@Model.CountryId">@Model.Country?.NameTr</a></li>
                    <li class="breadcrumb-item active">@Model.NameTr</li>
                </ol>
            </nav>
            <h2 class="text-primary"><i class="fas fa-city me-2"></i>@LocalizationService.GetString("Districts") <small class="text-muted">(@Model.NameTr)</small></h2>
        </div>
        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#createModal">
             <i class="fas fa-plus me-2"></i> @LocalizationService.GetString("AddDistrict")
        </button>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <table id="districtsTable" class="table table-hover table-striped">
                <thead>
                    <tr>
                        <th>@LocalizationService.GetString("NameTr")</th>
                        <th>@LocalizationService.GetString("NameEn")</th>
                        <th>@LocalizationService.GetString("NameAr")</th>
                        <th class="text-end">@LocalizationService.GetString("Actions")</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Districts)
                    {
                        <tr>
                            <td class="fw-bold">
                                <a asp-action="DistrictDetails" asp-route-id="@item.Id" class="text-decoration-none">
                                    @item.NameTr
                                </a>
                            </td>
                            <td>@item.NameEn</td>
                            <td>@item.NameAr</td>
                            <td class="text-end">
                                <button class="btn btn-sm btn-outline-warning me-1" onclick="openEditModal('@item.Id')">
                                    <i class="fas fa-edit"></i>
                                </button>
                                <form asp-action="DeleteDistrict" asp-route-id="@item.Id" method="post" class="d-inline" onsubmit="return confirm('@LocalizationService.GetString("DeleteConfirmation")');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger"><i class="fas fa-trash"></i></button>
                                </form>
                                <a asp-action="DistrictDetails" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary ms-1">
                                    <i class="fas fa-arrow-right"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- Create Modal -->
<div class="modal fade" id="createModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">@LocalizationService.GetString("AddDistrict")</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form asp-action="CreateDistrict" method="post">
                <input type="hidden" name="CityId" value="@Model.Id" />
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label">@LocalizationService.GetString("NameTr")</label>
                        <input type="text" class="form-control" name="NameTr" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">@LocalizationService.GetString("NameEn")</label>
                        <input type="text" class="form-control" name="NameEn">
                    </div>
                    <div class="mb-3">
                        <label class="form-label">@LocalizationService.GetString("NameAr")</label>
                        <input type="text" class="form-control" name="NameAr" dir="rtl">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Edit Modal -->
<div class="modal fade" id="editModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">@LocalizationService.GetString("EditDistrict")</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form asp-action="EditDistrict" method="post">
                <input type="hidden" name="Id" id="edit_Id" />
                <div class="modal-body">
                    <div class="mb-3">
                        <label class="form-label">@LocalizationService.GetString("NameTr")</label>
                        <input type="text" class="form-control" name="NameTr" id="edit_NameTr" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">@LocalizationService.GetString("NameEn")</label>
                        <input type="text" class="form-control" name="NameEn" id="edit_NameEn">
                    </div>
                    <div class="mb-3">
                        <label class="form-label">@LocalizationService.GetString("NameAr")</label>
                        <input type="text" class="form-control" name="NameAr" id="edit_NameAr" dir="rtl">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Update</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/dataTables.bootstrap5.min.css">
    <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>

    <script>
        $(document).ready(function() {
            $('#districtsTable').DataTable({
                "language": {
                     "url": "//cdn.datatables.net/plug-ins/1.13.6/i18n/tr.json"
                }
            });
        });

        function openEditModal(id) {
            $.get('/Locations/GetDistrict/' + id, function(data) {
                $('#edit_Id').val(data.id);
                $('#edit_NameTr').val(data.nameTr);
                $('#edit_NameEn').val(data.nameEn);
                $('#edit_NameAr').val(data.nameAr);
                
                var myModal = new bootstrap.Modal(document.getElementById('editModal'));
                myModal.show();
            });
        }
    </script>
}
